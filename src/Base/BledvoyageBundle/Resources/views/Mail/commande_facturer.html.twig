{% trans with { 
    '%prenom%':     product.prenom,
    '%id%':         product.id,
    '%ticket%':     product.ticket,
    '%nom%':        product.nom,
    '%tel%':        product.tel,
    '%email%':      product.email,
    '%datetime%':   product.dateTime|date('d/m/Y'),
    '%tarif%':      product.tarif,
    '%nombre%':     product.nombre,
    '%paiement%':   product.paiement,
    '%total%':      product.total
} %}base.bledvoyage.mail.commande_facturer{% endtrans %}
