{% trans with { 
    '%id%':             product.id,
    '%secondename%':    product.user.secondename,
    '%firstname%':      product.user.firstname,
    '%nom%':            product.categorieTicket.nom,
    '%datetime%':       product.dateTime|date('d/m/Y'),
    '%tarif%':          product.categorieTicket.tarif,
    '%nombre%':         product.nombre,
    '%duree%':          product.categorieTicket.duree,
    '%description%':    product.categorieTicket.description,
    '%conditions%':     product.categorieTicket.conditions
} %}base.bledvoyage.mail.att_commande{% endtrans %}
