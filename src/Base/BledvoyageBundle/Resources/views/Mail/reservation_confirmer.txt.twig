{% trans with { 
    '%prenom%':     product.prenom,
    '%id%':         product.id,
    '%sortie%':     product.sortie,
    '%datetime%':   product.dateTime|date('d/m/Y'),
    '%tarif%':      product.tarif,
    '%heureDebut%': product.heureDebut,
    '%nombre%':     product.nombre,
    '%creneau%':    product.creneau,
    '%condition%':  product.condition,
    '%plan%':       product.planAcces,
    '%moniteur%':   product.moniteur
} %}base.bledvoyage.mail.conf_reservation{% endtrans %}
