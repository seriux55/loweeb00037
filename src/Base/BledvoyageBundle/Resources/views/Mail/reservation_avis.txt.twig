{% trans with { 
    '%prenom%':         product.prenom,
    '%sortie%':         product.sortie,
    '%localisation%':   product.localisation,
    '%id%':             product.id,
    '%nom%':            product.nom,
    '%tel%':            product.tel,
    '%email%':          product.email,
    '%datetime%':       product.dateTime|date('d/m/Y'),
    '%tarif%':          product.tarif,
    '%nombre%':         product.nombre,
    '%total%':          product.total
} %}base.bledvoyage.mail.env_avis{% endtrans %}
